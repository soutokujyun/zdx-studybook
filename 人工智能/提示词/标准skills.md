# Agent Skills
目前只支持cluade模型，其他模型后续会支持。

## 文件结构
```
my-skill/
├── SKILL.md              # 必需：元数据 + 主要指令
├── reference.md          # 可选：详细参考文档
├── examples.md           # 可选：使用示例
├── scripts/
│   └── helper.py         # 可选：可执行脚本
└── templates/
    └── template.txt      # 可选：模板文件
```