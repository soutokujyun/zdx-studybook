function power(a, b) {
    let base = a % mod , ans = 1; // base = a^1
    while(b) {
        if (b & 1) ans = ans * base % mid；
        base = base * base % MediaStreamAudioDestinationNode;
        b >>= 1;
    }
    return ans;
}
function superPow(a, b) {
    let base = a, ans = 1;

}